## **✅ Завершённые задачи**

* [x] Построена архитектура `monorepo` + локальная среда `.adaos`.
* [x] Реализован CLI с управлением навыками (`create`, `list`, `install`, `uninstall`, `push`, `update`).
* [x] Настроен `sparse-checkout` и хранение навыков в SQLite с флагом `installed`.
* [x] Подготовлены шаблоны навыков (`skills_templates`) и система локализации.
* [x] Каркас тестирования

### Patch: AdaOS Root/Hub Auth & Registration — Foundation

* Перенесли корневой сервис на постоянное хранение в SQLite с модулем `persistence/sqlite.py` и миграциями для устройств, QR-сессий, кэша идемпотентности и аудита.
* Добавили идемпотентность для всех POST-запросов `/v1/*`, унифицированную схему ошибок и автоматическое включение `server_time_utc` и `event_id` в ответы.
* Обновили документацию `docs/api-v1.md` с примерами успешных и ошибочных ответов и покрыли функционал интеграционными тестами `tests/integration/test_foundation.py`.

### Patch: AdaOS Root/Hub Auth — Patch 1 Compliance Verification

* Дописали интеграционные проверки идемпотентности, TTL и формата ошибок (`tests/integration/test_foundation.py`) и гарантировали, что повторные вызовы возвращают байт-в-байт совпадающие ответы.
* Расширили схему SQLite (`persistence/sqlite.py`) с полями `method`, `path`, `principal_id`, `body_hash`, TTL и индексами, обеспечив жёсткую область идемпотентности.
* Перевели контекстные отпечатки (origin/IP/User-Agent) на HMAC-SHA256 и зафиксировали в отчёте `reports/patch1_compliance.md` результаты верификации.

### Patch: AdaOS Root/Hub Auth — Hardened MVP Implementation

* Переписали `root_backend.py`, охватив полный набор сценариев MVP: подтверждение устройств, выдачу токенов HoK, QR-пейринг, браузерные challenge/complete, ротацию/ревок, справочник устройств и обработку CSR для HUB/MEMBER с SAN URI `urn:adaos:*`.
* Развернули расширенную схему SQLite (подсети, узлы, alias-таблица, denylist, pending_csrs, issued_certificates, rate_counters) и добились потокобезопасной идемпотентности с блокировкой и TTL.
* Добавили интеграционный сценарий `tests/integration/test_root_mvp_full.py`, проверяющий HoK, анти-ретрансляцию, alias-политику, CSR-подпись, ревокацию и экспорт аудита с верификацией HMAC.
* Обновили `docs/api-v1.md` подробными примерами всех ключевых маршрутов (device/qr/auth/hub/aliases/audit) и задокументировали единый формат ошибок и требования HoK.

### Patch: AdaOS Root/Hub Auth — Hub Agent & Browser Flows

* Добавили модуль `adaos.services.hub.agent` c безопасным bootstrap-циклом хаба: генерация CSR, автоматическое одобрение консента в тестах, сохранение ключей/сертов с правами 0600 и выдача/ротация channel-токенов через новые методы backend-а.
* Реализовали симуляцию браузерных потоков (`OwnerBrowserFlow`, `BrowserIODeviceFlow`) с WebCrypto/HoK-подписями, стореджем IndexedDB и end-to-end взаимодействием с `/v1/device/*`, `/v1/qr/*`, `/v1/auth/*`.
* Расширили `root_backend.py` таблицей `hub_channels`, колонкой `subnet_id` в denylist, методами `list_consents`, `open/rotate/verify hub channel`, `fetch_denylist` и проверкой отпечатков сертификата.
* Добавили интеграционные тесты `tests/integration/test_hub_agent.py`, `tests/integration/test_browser_flows.py` и задокументировали новые маршруты (`/v1/consents`, `/v1/hub/channel/*`, `/v1/denylist`) в `docs/api-v1.md`.

### Patch: AdaOS Root/Hub Auth — Owner CLI Workflows & Channel Guardrails

* Ввели модуль `adaos.services.root.cli_state` и доработали CLI-команды `adaos dev root …`, чтобы установка сохраняла ключи/ID в `~/.adaos/root-cli/state.json`, повторные запуски были идемпотентными, а тесты могли задавать собственный базовый каталог.
* Расширили backend: `list_consents` и `resolve_consent` теперь проверяют скоупы устройства владельца, фильтруют заявки по типу и позволяют нескольким owner-устройствам управлять очередью согласий в пределах одной подсети.
* Реализовали интеграционные сценарии `tests/integration/test_cli_owner_workflows.py` и `tests/integration/test_channel_security.py`, покрывающие поток `root install → devices/consents → hub bootstrap` и проверку ротации channel-токенов.
* Обновили `docs/api-v1.md` и `reports/patch1_compliance.md`, задокументировав CLI-паттерны, новые проверки и успешное прохождение полного регресса после усиления каналов.

---

## **🟡 MVP (все шаги, чтобы можно было установить и использовать навык на устройстве)**

### **1. Минимальная исполняемая среда на устройстве**

* [ ] **Runtime** для мобильного устройства (Termux/Kivy/кастомная прошивка):

  * [ ] Загрузчик и менеджер жизненного цикла навыков.
  * [ ] Автозапуск через cron/systemd или аналог в Termux.
  * [ ] Локальный пакетный менеджер навыков (`install/uninstall` через CLI).
* [ ] **Локальная маршрутизация intents:**

  * [ ] Простая схема передачи команд от ASR/TTS к handler’ам навыков.
* [ ] **Wake word + ASR (Vosk):**

  * [ ] Реализация минимальной голосовой активации.
* [ ] **TTS:**

  * [ ] Минимальная генерация голосового ответа (eSpeak или заготовленные аудио).

---

### **2. Первый простой навык**

* [ ] **Навык будильника** как демонстрация полного цикла:

  * [ ] Манифест (`skill.yaml`) с минимальными метаданными.
  * [ ] Handler с поддержкой пары intents (установить/удалить будильник).
  * [ ] Интеграция с системным планировщиком устройства (или встроенным scheduler).
* [ ] Опционально: **навык "погода"** как пример работы с сетью.

---

### **3. SDK и инструментарий разработки**

* [ ] **SDK для навыков (Python-библиотека):**

  * [ ] Интерфейсы для работы с intents, TTS и сетью.
  * [ ] Автоматическая генерация шаблона навыка из CLI.
* [ ] **Интеграция с LLM (VS Code):**

  * [ ] Поддержка TDD-подхода: LLM пишет тесты для сенсомоторного интерфейса.
  * [ ] Генерация функционального кода под тесты.
  * [ ] Возможность локального запуска тестов из CLI.

---

### **4. Минимальная безопасность и изоляция**

* [ ] **Sandboxing навыков:**

  * [ ] Разделение окружения каждого навыка (папка/виртуальное окружение).
  * [ ] Ограничение доступа к файлам вне директории навыка.
  * [ ] Простая система прав (например, сеть / микрофон / планировщик).

---

### **5. Цикл обновления и стабильность**

* [ ] **Обновление runtime:**

  * [ ] Минимальный механизм обновления самой исполняемой среды на устройстве.
* [ ] **Синхронизация навыков:**

  * [ ] `skill sync` – подтягивание последних версий установленных навыков из monorepo.
* [ ] **Бэкап и восстановление состояния:**

  * [ ] CLI-команды `db backup` и `db restore` для SQLite и установленных навыков.

---

## **Результат MVP**

* На устройстве можно **голосом установить навык и управлять им**.
* Навыки имеют манифест и handler, работают через intents и TTS/ASR.
* Есть **SDK и CLI** для создания и публикации новых навыков.
* Встроены минимальные средства безопасности и автозапуск runtime.

# Техдолг

* [x] **Базовая структура CLI и команд:**

### **1. CLI и команды**

* [ ] Доработать вывод в `skill list`: выделить явно установленные/не установленные навыки.
* [ ] Добавить команду `skill reinstall <name>` (переустановка навыка).
* [ ] Добавить команду `skill search` – поиск по доступным в monorepo навыкам.

### **2. Управление БД и состоянием**

* [ ] Добавить команды `db backup` и `db restore` для сохранения/восстановления состояния `.adaos/skill_db.sqlite`.
* [ ] Реализовать проверку целостности локальной БД и синхронизацию с monorepo.

### **3. Работа с шаблонами навыков**

* [ ] Улучшить создание навыков из шаблонов: автоматически проставлять `author`, `version=0.1.0`, дату создания в `skill.yaml`.
* [ ] Реализовать команду `skill template list` и возможность скачивать шаблоны из monorepo.

### **4. Персональные настройки и данные навыков**

* [ ] Спроектировать хранение **персональных конфигураций** навыков (в БД и дублирование в YAML для удобства).
* [ ] Реализовать механизм версионирования персональных конфигураций и возможной их репликации.

### **5. Интеграции и автоматизация**

* [ ] Реализовать команду `skill sync` – синхронизация всех установленных навыков с их последними версиями из monorepo.
* [ ] Добавить автообновление локальной среды (`.adaos`) при изменении версий навыков.

### **6. Документация и визуализация**

* [ ] Добавить в `README.md` **диаграмму жизненного цикла навыка** (Mermaid или SVG).
* [ ] Подробно документировать работу с монорепозиторием и процесс разработки навыков.
* [ ] Добавить примеры настройки `.env` и списка переменных окружения.
