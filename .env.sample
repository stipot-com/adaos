# === Core runtime settings ===
ENV_TYPE=dev                     # dev|prod - selects default base dir layout
ADAOS_LANG=en                    # Default UI/CLI language (en|ru)
ADAOS_PROFILE=default            # Active AdaOS profile name
ADAOS_BASE_DIR=                  # Override base directory (absolute path); empty -> auto
ADAOS_BASE_DIR_SUFFIX=           # Optional suffix for .adaos directory in dev mode
BASE_DIR=                        # Force base dir (fallback when ADAOS_BASE_DIR not set)

# === Logging & diagnostics ===
ADAOS_CLI_DEBUG=0                # Print stack traces in CLI on failure when set to 1/true
ADAOS_SCENARIO_LOG_LEVEL=INFO    # Scenario execution log level
ADAOS_TESTING=0                  # Enable testing shortcuts/mocks when 1
ADAOS_READY_PATH=                # Optional file path toggled when services become ready

# === Build metadata overrides ===
ADAOS_BUILD_VERSION=             # Optional override for Git-derived build version (set in CI)
ADAOS_BUILD_DATE=                # Optional override for build timestamp (ISO8601)
ADAOS_BASE_VERSION=0.1.0         # Base semantic version used when deriving build numbers

# === SDK / skills configuration ===
ADAOS_SDK_EXPORT_MODULES=        # Comma-separated modules allowed for SDK exporter override
ADAOS_SKILL_ENV_PATH=            # Path to custom skill environment (venv)
ADAOS_SKILL_TEST_ENV_PREFIX=     # Prefix for on-demand skill test virtualenvs
ADAOS_ALLOW_UNSAFE_MONOREPO=0    # Allow overriding monorepo URLs/branches when 1
ADAOS_SKILLS_MONOREPO_URL=       # Custom skills monorepo URL (requires unsafe override)
ADAOS_SKILLS_MONOREPO_BRANCH=    # Custom skills branch (requires unsafe override)
ADAOS_SCENARIOS_MONOREPO_URL=    # Custom scenarios monorepo URL (requires unsafe override)
ADAOS_SCENARIOS_MONOREPO_BRANCH= # Custom scenarios branch (requires unsafe override)

# === Testing / sandbox ===
ADAOS_MIN_PY=3.11                # Minimum Python version required for tests
ADAOS_TEST_PY=                   # Specific Python executable for tests (e.g. python3.11)
ADAOS_SANDBOX_DISABLED=0         # Disable sandbox execution when 1 (unsafe)

# === Gateway / API integration ===
ADAOS_BASE=http://127.0.0.1:8777 # Base URL for local AdaOS API bridge
ADAOS_TOKEN=dev-local-token      # X-AdaOS-Token used for local API calls
ADAOS_CMD=                       # Override AdaOS launch command (CLI/testing helpers)
ADAOS_GW_PORT=8777               # Gateway port exposed by local AdaOS bridge
ADAOS_TARGET_HOST=127.0.0.1      # Hostname of AdaOS target when tunneling traffic
ADAOS_TARGET_PORT=8777           # Target port of AdaOS when tunneling traffic

# === External services ===
OPENAI_API_KEY=sk-XXXXXXXXXXXXXXXXXXXX   # API key for OpenAI integrations

# === Voice assistants / speech ===
ADAOS_RHASSPY_URL=http://127.0.0.1:12101 # Rhasspy base URL
ADAOS_RHASSPY_CONTAINER=adaos-rhasspy    # Docker container name for Rhasspy helper
ADAOS_RHASSPY_IMAGE=rhasspy/rhasspy:latest
ADAOS_RHASSPY_PORT=12101                 # Host port for Rhasspy web UI/API
ADAOS_RHASSPY_PROFILE=en                 # Rhasspy profile/language
ADAOS_RHASSPY_DATA=~/.config/rhasspy     # Host directory for Rhasspy data
ADAOS_VOSK_MIRROR=                       # Optional mirror URL for Vosk models
ADAOS_VOSK_ZIP=                          # Local path or URL to pre-downloaded Vosk archive

# === Messaging / storage ===
MQTT_HOST=mqtt                   # MQTT broker hostname
MQTT_PORT=1883                   # MQTT broker port
SQLITE_PATH=/app/skill_db.sqlite # Path to SQLite database with skills metadata

# === Git identity ===
GIT_USER=adaos                   # Default git author name for generated commits
GIT_EMAIL=adaos@local            # Default git author email

# === Platform specific (auto-detected if unset) ===
KIVY_BUILD=                      # Internal flag used by Kivy packaging (android)
LOCALAPPDATA=                    # Windows-specific override for local data path
