This directory contains nginx `vhost.d` snippets for `nginxproxy/nginx-proxy`.

Files are matched by server name (for example, `vhost.d/api.inimatic.com`) and are
injected into the generated `server {}` block.
